import 'package:flutter/material.dart';

class PopupTaskInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Empty'),

    );
  }
}